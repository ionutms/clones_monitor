name: Monitor Repository Clones

on:
  schedule:
    # Run every hour
    - cron: '0 * * * *'
  workflow_dispatch:  # Allow manual triggering

jobs:
  monitor-clones:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout clones_monitor repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch all history for all branches and tags
      
    - name: Setup GitHub CLI
      uses: sersoft-gmbh/setup-gh-cli-action@v2.0.1
      
    - name: Fetch clone statistics
      env:
        GH_TOKEN: ${{ secrets.CLONE_MONITOR_TOKEN }}
      run: |
        # Fetch current timestamp in year/month/day format when zeroes
        timestamp=$(date -u +"%Y/%m/%d")
        
        # Ensure CSV exists with header if it doesn't
        if [ ! -f clones_history.csv ]; then
          echo "collection_timestamp,clone_timestamp,total_clones,unique_clones" > clones_history.csv
        fi
        
        # Fetch clones for the public repository and directly parse to CSV
        gh api repos/ionutms/KiCAD_Symbols_Generator/traffic/clones | \
        jq -r --arg timestamp "$timestamp" '
          .clones | 
          map([
            $timestamp, 
            (.timestamp | 
              gsub("-"; "/") | 
              gsub("Z$"; "") | 
              split("T")[0]),
            .count, 
            .uniques
          ] | @csv)[] 
        ' | while IFS=, read -r _ clone_date total_clones unique_clones; do
          # Check if this exact entry already exists in the CSV
          if ! grep -q "$clone_date,$total_clones,$unique_clones" clones_history.csv; then
            echo "$timestamp,$clone_date,$total_clones,$unique_clones" >> clones_history.csv
          fi
        done
    
    - name: Commit and push clone data
      run: |
        git config user.name 'GitHub Actions Bot'
        git config user.email '<>'
        
        # Stage only the CSV file
        git add clones_history.csv
        
        # Check if there are any changes to commit
        if [[ -n $(git status -s) ]]; then
          git commit -m "Update clone statistics for $(date -u +"%Y-%m-%d")"
          git pull --rebase origin main
          git push origin main
        else
          echo "No new clone data to update"
        fi